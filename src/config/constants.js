// Core dimensions and non-tunable constants.
export const GAME_WIDTH = 1800;
export const GAME_HEIGHT = 1200;

// The playable world. Today it equals the viewport so the camera shows the
// whole arena exactly as before; widening these later (plus a camera follow)
// is how the gamespace "opens up" beyond the window.
export const WORLD_WIDTH = 1800;
export const WORLD_HEIGHT = 1200;

export const FONT_FAMILY = '"Orbitron", sans-serif';
export const MAX_HP = 5;
